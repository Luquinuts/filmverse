# openspec/config.yaml — FilmVerse SDD Configuration
# Auto-generated by sdd-init on 2026-06-06
schema: spec-driven

context: |
  FilmVerse: plataforma web para cinéfilos — proyecto académico ISW III,
  Universidad Atlántida Argentina. Stack objetivo: PHP/MySQL/Apache.
  Estado: sin código; fase de diseño/prototipado con documentación en
  /references/. Arquitectura definida en los requerimientos (ERS IEEE 830).
  Sin test runner, linter, type checker ni formatter disponibles.

rules:
  proposal:
    - Incluir alcance MVP y limitaciones del proyecto (no implementación real)
    - Alinear con la planificación del TP (Jira/Google Docs)
  specs:
    - Usar Given/When/Then para escenarios
    - Usar keywords RFC 2119 (DEBE, DEBERÍA, PUEDE)
    - Referenciar RF/RNF del ERS en /references/
  design:
    - Incluir diagramas de secuencia para flujos complejos
    - Documentar decisiones de diseño con rationale
    - Referenciar diagrama de clases existente en /references/
  tasks:
    - Agrupar por fase, usar numeración jerárquica
    - Tareas deben ser completables en una sesión
    - Alinear con el tablero de Jira del grupo
  apply:
    - Seguir patrones existentes de prototipado
    tdd: false
    test_command: ""
  verify:
    - Verificación contra requerimientos del ERS
    test_command: ""
    build_command: ""
    coverage_threshold: 0
  archive:
    - Advertir antes de fusionar deltas destructivos

testing:
  strict_tdd: false
  reason: "Sin test runner disponible (proyecto sin código). TDD no aplica en fase de diseño/prototipado."
  detected: "2026-06-06"
  runner:
    command: ""
    framework: ""
  layers:
    unit: { available: false, tool: "" }
    integration: { available: false, tool: "" }
    e2e: { available: false, tool: "" }
  coverage:
    available: false
    command: ""
  quality:
    linter: { available: false, command: "" }
    type_checker: { available: false, command: "" }
    formatter: { available: false, command: "" }
